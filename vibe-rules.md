# 🌌 Vibe Coding Rules (개발 및 배포 지침서)

이 파일은 에이전트(AI)가 작업을 수행할 때 반드시 준수해야 하는 행동 지침 및 설정 파일입니다. 앞으로 프로젝트를 진행하며 새로운 원칙이나 주의사항이 생길 때마다 이 파일에 계속 보충하여 저장합니다.

---

## 1. 0원 무료 배포 방어 수칙 (GCP Always Free Tier)
GCP 리소스를 사용할 때 의도하지 않은 과금을 방지하기 위한 핵심 규칙입니다.

* **배포 리전 (Region)**: GCE VM 인스턴스 및 Cloud Run 서비스 배포 시 반드시 미국 리전 중 하나인 **`us-central1` (아이오와)**를 선택해야 합니다. (서울 리전은 무료 대상이 아닙니다.)
* **Cloud Run 최소 인스턴스**: 접속이 없을 때 비용이 발생하지 않도록 항상 최소 인스턴스(min-instances)를 **`0`**으로 설정합니다.
* **Cloud Run 최대 인스턴스**: 비정상적인 호출 폭증으로 인한 요금 테러를 방지하기 위해 최대 인스턴스(max-instances)를 **`5`** 이하로 제한합니다.
* **VM 머신 및 디스크 제한**: Compute Engine 인스턴스는 오직 계정당 1대의 **`e2-micro`**만 무료로 구동할 수 있으며, 부팅 디스크는 **`30GB` 이하의 표준 영구 디스크(Standard HDD)**로 제한합니다.
* **무료 외부 데이터베이스 사용**: 상시 과금이 일어나는 GCP Cloud SQL 대신 **Supabase의 무료 PostgreSQL**을 연동하여 개발합니다.

---

## 2. 모노레포(Monorepo) 통합 관리 수칙
프로젝트마다 깃허브 저장소를 새로 생성해야 하는 노가다를 방지하고 일관된 코드 품질을 유지하기 위한 개발 수칙입니다.

* **저장소 단일화**: 새로운 기능이나 프로젝트를 만들 때마다 개별 저장소를 새로 파지 않고, 메인 저장소인 `pgcz`의 하위 폴더(예: `/gravity-cash`, `/vibe-coding-planner`)로 격리 배치하여 한 번에 Push합니다.
* **서브 디렉터리 빌드 설정**: Cloud Run 및 깃허브 연동 시, 각 서비스가 소속 폴더 내부의 Dockerfile만 바라보고 빌드되도록 루트 컨텍스트 경로 설정을 개별 폴더명으로 지정하도록 가이드합니다.
* **사전 빌드 자가검증**: 코드를 깃허브에 Push하기 전, 에이전트가 로컬 터미널에서 `npm run build` 등의 컴파일 명령을 자발적으로 실행하여 런타임에 에러가 없음을 먼저 증명해야 합니다.
